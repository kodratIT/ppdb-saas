# Stores

Svelte stores for application state management.

## Purpose
- Centralized state management using Svelte's writable, readable, and derived stores
- Reactive state that can be accessed across components
- Business logic related to data persistence and state transitions

## Usage
```typescript
import { userStore } from '@/lib/stores/userStore';
```